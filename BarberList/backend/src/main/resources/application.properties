spring.application.name=BarberList

# PostgreSQL (configured via environment variables for production)
spring.datasource.url=${DB_URL:jdbc:postgresql://db.wlslcexchqpzlufjqgjt.supabase.co:5432/postgres}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:jurassicpark126mrv}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Servidor
server.port=${PORT:8080}
spring.web.resources.static-locations=classpath:/static/

# CORS já configurado via WebConfig.java
